{
  "permissions": {
    "allow": [
      "Bash(npx next:*)",
      "Bash(npx prisma:*)",
      "Bash(npm run:*)"
    ]
  },
  "notes": "This is an example settings file. Create a local .claude/settings.local.json for development and DO NOT commit secrets."
}
