# CSV Format Robustness Audit — 2026-04-23

Generated by `scripts/audit-csv-parsers.ts`. Re-run to regenerate.

## Summary

Ran 12 CSV variants against 5 shape parsers. 40/56 applicable (shape, variant) pairs produce output semantically equivalent to the baseline. PARTIAL: 10, FAIL_DETECT: 3, FAIL_PARSE: 3, N/A: 4.

Cell legend: `✓` PASS · `~` PARTIAL · `✗D` FAIL_DETECT · `✗P` FAIL_PARSE · `–` N/A.

## Matrix

| Variant | Description | variance | ar | gl | sub_ledger | fx |
|---------|-------------|---|---|---|---|---|
| A1 | Swap column order (reverse) | ✓ | ✓ | ✓ | ✓ | ✓ |
| A2 | Rename with synonym | ✓ | ✓ | ✗D | ✗D | ✗D |
| A3 | Uppercase headers | ✓ | ✓ | ✓ | ✓ | ✓ |
| A4 | Whitespace + title-case (underscores -> spaces) | ✓ | ✓ | ✗P | ✗P | ✗P |
| A5 | Add irrelevant extra column 'Notes' | ✓ | ✓ | ✓ | ✓ | ✓ |
| A6 | Drop one optional column | ✓ | ✓ | ✓ | ✓ | – |
| B1 | Date format: ISO -> US (MM/DD/YYYY) | – | ✓ | ✓ | ✓ | ✓ |
| B2 | Date format: ISO -> EU (DD/MM/YYYY) | – | ✓ | ✓ | ✓ | ✓ |
| B3 | Date format: named month (15 Apr 2026) | – | ~ | ✓ | ✓ | ✓ |
| B4 | Amount with thousands separator (1,234.56) | ✓ | ✓ | ~ | ~ | ✓ |
| B5 | Amount with accounting parens for negatives ((500.00)) | ✓ | ~ | ~ | ~ | ~ |
| B6 | Amount with currency-symbol prefix ($1234.56) | ✓ | ✓ | ~ | ~ | ~ |

## Per-shape findings

### variance

All applicable variants PASS. No gaps.

### ar

Failures: 2

- **B3** (PARTIAL) — Date format: named month (15 Apr 2026). rowCount 0/3, key (null) vs INV-001
- **B5** (PARTIAL) — Amount with accounting parens for negatives ((500.00)). rowCount 2/3, key INV-002 vs INV-001

### gl

Failures: 5

- **A2** (FAIL_DETECT) — Rename with synonym. detectFastPath returned 'unknown' (expected 'gl')
- **A4** (FAIL_PARSE) — Whitespace + title-case (underscores -> spaces). GL CSV missing required headers
- **B4** (PARTIAL) — Amount with thousands separator (1,234.56). rowCount 1/3, key GL-003 vs GL-001
- **B5** (PARTIAL) — Amount with accounting parens for negatives ((500.00)). rowCount 2/3, key GL-002 vs GL-001
- **B6** (PARTIAL) — Amount with currency-symbol prefix ($1234.56). rowCount 0/3, key (null) vs GL-001

### sub_ledger

Failures: 5

- **A2** (FAIL_DETECT) — Rename with synonym. detectFastPath returned 'unknown' (expected 'sub_ledger')
- **A4** (FAIL_PARSE) — Whitespace + title-case (underscores -> spaces). Sub-ledger CSV missing required headers
- **B4** (PARTIAL) — Amount with thousands separator (1,234.56). rowCount 1/3, key GL-003 vs GL-001
- **B5** (PARTIAL) — Amount with accounting parens for negatives ((500.00)). rowCount 2/3, key GL-002 vs GL-001
- **B6** (PARTIAL) — Amount with currency-symbol prefix ($1234.56). rowCount 0/3, key (null) vs GL-001

### fx

Failures: 4

- **A2** (FAIL_DETECT) — Rename with synonym. detectFastPath returned 'unknown' (expected 'fx')
- **A4** (FAIL_PARSE) — Whitespace + title-case (underscores -> spaces). FX CSV missing required headers
- **B5** (PARTIAL) — Amount with accounting parens for negatives ((500.00)). rowCount 2/3, key GBP vs EUR
- **B6** (PARTIAL) — Amount with currency-symbol prefix ($1234.56). rowCount 0/3, key (null) vs EUR


## Punch-list

Shapes ordered by failing-variant count. Each listed variant is a concrete target for a future robustness fix.

- `gl`: 5 failing variants — A2, A4, B4, B5, B6
- `sub_ledger`: 5 failing variants — A2, A4, B4, B5, B6
- `fx`: 4 failing variants — A2, A4, B5, B6
- `ar`: 2 failing variants — B3, B5

## How to interpret

- **PASS** means the parser produced output with the same row count and the same key field as the baseline (variance=account, ar=invoiceNumber, gl/sub=reference, fx=fromCurrency).
- **PARTIAL** means the parser returned output but with a different row count or key field. For most shapes this is the `skipped[]` array path (rows that couldn't be parsed get collected there). For FX it's a silent drop because `fx-rates-parser.ts:16` continues on bad rows without tracking them.
- **FAIL_DETECT** means `detectFastPath` returned the wrong shape for the mutated headers. The audit does not exercise the LLM fallback; in production an LLM inference would attempt recovery.
- **FAIL_PARSE** means the parser threw.
